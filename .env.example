# SVN Server Configuration
# Copy this file to .env and customize the values

# Admin user credentials
SVN_ADMIN_USER=admin
SVN_ADMIN_PASSWORD=changeme123

# Server configuration
APACHE_SERVER_NAME=svn.localhost

# Examples for production:
# SVN_ADMIN_USER=svnadmin
# SVN_ADMIN_PASSWORD=your-secure-password-here
# APACHE_SERVER_NAME=svn.yourdomain.com

# Security recommendations:
# - Use a strong password (min 12 characters)
# - Include uppercase, lowercase, numbers and special characters
# - Change the default admin username
# - Use your actual domain name for APACHE_SERVER_NAME

# Note: SSL is handled by nginx proxy manager
# Configure your domain in nginx proxy manager to point to svn-server:80
